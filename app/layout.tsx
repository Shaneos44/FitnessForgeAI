import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { AuthProvider } from "@/components/auth-provider"
import { ThemeProvider } from "@/components/theme-provider"
import { Toaster } from "@/components/ui/toaster"
import { ErrorBoundary } from "@/components/error-boundary"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "FitnessForge AI - Your Personal AI Fitness Coach",
  description:
    "Transform your fitness journey with AI-powered personalized workout plans, real-time coaching, and comprehensive progress tracking.",
  keywords: "fitness, AI, workout, personal trainer, health, exercise",
  authors: [{ name: "FitnessForge AI" }],
  openGraph: {
    title: "FitnessForge AI - Your Personal AI Fitness Coach",
    description: "Transform your fitness journey with AI-powered personalized workout plans",
    type: "website",
  },
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ErrorBoundary>
          <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
            <AuthProvider>
              {children}
              <Toaster />
            </AuthProvider>
          </ThemeProvider>
        </ErrorBoundary>
      </body>
    </html>
  )
}
